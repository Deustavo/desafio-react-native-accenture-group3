import styled from 'styled-components/native';
import { LinearGradient } from 'expo-linear-gradient';

export const HideContainer = styled(LinearGradient)`
    height: 20px;
    border-radius: 5px;
    margin: 10px auto;
`;
